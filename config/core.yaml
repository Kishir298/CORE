core:
  name: "C.O.R.E."
  version: "0.2.0"

environment: "development"

logging:
  level: "INFO"

communication:
  enabled: true

network:
  enabled: false

components:
  configuration:
    enabled: true
  logging:
    enabled: true
  security:
    enabled: true
  resources:
    enabled: true
  organization:
    enabled: true
  events:
    enabled: true
  communication:
    enabled: true
  routing:
    enabled: true
  health:
    enabled: true
  dependencies:
    enabled: true
  services:
    enabled: true